<!DOCTYPE html>
<html>
  <head>
    <title>DrMovil</title>
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag    'application', media: 'all' %>
    <%= javascript_include_tag 'application' %>
  </head>

  <body>
    <% if patient_signed_in? %>
      <%= link_to "Logout", destroy_patient_session_path, method: :delete, :class => 'navbar-link'  %>
    <% elsif doctor_signed_in? %>
      <%= link_to "Logout", destroy_doctor_session_path, method: :delete, :class => 'navbar-link'  %>
    <% else %>
      <%= link_to "Sign up Pacientes", new_patient_registration_path, :class => 'navbar-link'  %> |
      <%= link_to "Iniciar Sesión Pacientes", new_patient_session_path, :class => 'navbar-link'  %>
      <br>
      <%= link_to "Sign up Doctores", new_doctor_registration_path, :class => 'navbar-link'  %> |
      <%= link_to "Iniciar Sesión Doctores", new_doctor_session_path, :class => 'navbar-link'  %>
    <% end %>
    <p class="notice"><%= notice %></p>
    <p class="alert"><%= alert %></p>
    <%= yield %>
  </body>
</html>
